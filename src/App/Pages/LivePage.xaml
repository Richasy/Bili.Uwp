<Page
    x:Class="Richasy.Bili.App.Pages.LivePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Richasy.Bili.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Richasy.Bili.App.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer
        x:Name="RootContainer"
        Padding="{StaticResource DefaultContainerWithBottomPadding}"
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutGroup">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideScreenThresholdWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.ColumnSpacing" Value="0" />
                        <Setter Target="FollowContainer.(Grid.Column)" Value="0" />
                        <Setter Target="FollowContainer.(Grid.Row)" Value="1" />
                        <Setter Target="FollowContainer.(Grid.RowSpan)" Value="1" />
                        <Setter Target="FollowContainer.Width" Value="Auto" />
                        <Setter Target="FollowView.ItemOrientation" Value="Vertical" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="PaddingGroup">
                <VisualState x:Name="DefaultPaddingState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource CompactModeThresholdWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowPaddingState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootContainer.Padding" Value="{StaticResource NarrowContainerWithBottomPadding}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid
            x:Name="RootGrid"
            ColumnSpacing="24"
            RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="Row1" Height="Auto" />
                <RowDefinition x:Name="Row2" Height="Auto" />
                <RowDefinition x:Name="Row3" Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="Column1" />
                <ColumnDefinition x:Name="Column2" Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid
                x:Name="BannerContainer"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                CornerRadius="{StaticResource OverlayCornerRadius}">
                <controls:BannerView ItemsSource="{x:Bind ViewModel.BannerCollection}" />
            </Grid>

            <Grid
                x:Name="FollowContainer"
                Grid.RowSpan="3"
                Grid.Column="1"
                HorizontalAlignment="Stretch">
                <controls:FollowLiveView x:Name="FollowView" ItemOrientation="Horizontal" />
            </Grid>

            <Grid
                x:Name="FeedsContainer"
                Grid.Row="2"
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
        </Grid>
    </ScrollViewer>
</Page>
